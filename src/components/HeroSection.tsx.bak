import { Shield } from "lucide-react";

export const HeroSection = () => {
  return (
    <section className="relative overflow-hidden py-20 px-6">
      <div className="absolute inset-0 bg-[var(--gradient-cyber)] opacity-30" />
      
      <div className="container mx-auto max-w-4xl text-center relative z-10">
        <div className="inline-flex items-center justify-center mb-6">
          <Shield className="w-16 h-16 text-primary" style={{ filter: 'drop-shadow(var(--glow-cyan))' }} />
        </div>
        
        <h1 className="text-5xl md:text-7xl font-bold mb-6 bg-gradient-to-r from-primary via-cyan-300 to-primary bg-clip-text text-transparent animate-pulse">
          Zer0trace
        </h1>
        
        <p className="text-xl md:text-2xl text-muted-foreground mb-4">
          Secure Certification Verification
        </p>
        
        <p className="text-foreground/70 max-w-2xl mx-auto">
          Upload your certification JSON file for instant verification against our secure database.
          Experience seamless authentication with zero compromise on security.
        </p>
      </div>
    </section>
  );
};
